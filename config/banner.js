import chalk from 'chalk';

const banner = `
${chalk.white('════════════════════════════════════════════════════════════════')}
                        ${chalk.bold.cyan('Taker Mining Tool')}
           ${chalk.yellow('-- Tool shared by GitHub user @rinehaxor --')}
                  ${chalk.magenta('-- Recover by @mobonchain --')}
${chalk.white('════════════════════════════════════════════════════════════════')}
`;

export default banner;
